document.getElementById('loginForm').addEventListener('submit', function(e) {
    e.preventDefault();
    // Here, you can add functionality to handle the login, like sending the data to the backend.
    alert('Login functionality can be added here!');
});
